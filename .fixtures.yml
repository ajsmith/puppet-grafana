fixtures:
  symlinks:
    grafana: "#{source_dir}"
  repositories:
    docker_systemd: "https://github.com/ajsmith/puppet-docker_systemd.git"
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref:  "4.5.1"
    docker:
      repo: "puppetlabs/docker_platform"
      ref:  "1.1.0"
    docker:
      repo: "garethr/docker"
      ref:  "5.0.0"
    vcsrepo:
      repo: "puppetlabs/vcsrepo"
      ref:  "1.3.1"
